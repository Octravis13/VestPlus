-- Criar o banco de dados se não existir
CREATE DATABASE IF NOT EXISTS vest_plus_db;

-- Usar o banco de dados
USE vest_plus_db;

-- Verificar que o banco foi criado
SELECT 'Banco de dados vest_plus_db criado com sucesso!' as status;
